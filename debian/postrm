#!/bin/sh

prefix=/usr/local
SHARE=$prefix/share/house

HAPP=housecgi

case $1 in
    remove)
        systemctl daemon-reload
        ;;
    purge)
        systemctl daemon-reload
        rm -f /etc/default/$HAPP
        rm -f /etc/house/githttp
        ;;
esac

